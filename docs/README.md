# GitHub Pages: docs/ bundle

1) Drag **docs/** into your repo and push.
2) Settings → Pages → Branch: **main**, Folder: **/docs**.
3) Optional: custom domain → add CNAME file.
4) PWA is enabled (manifest + sw.js).